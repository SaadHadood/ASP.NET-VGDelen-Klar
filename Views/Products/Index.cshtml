@model ProductsIndexViewModel

@{
    ViewData["Title"] = "Produckter";
    var products = await productService.GetAllAsync();
}

<div style="padding-top: 150px">
    @if (products != null)
    {
        <partial name="/Views/Partials/_GridCollection.cshtml" model="@Model.All" />
    }
</div>
