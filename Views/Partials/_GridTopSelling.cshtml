@model GridCollectionViewModel

@if (Model != null) 
{
    <section class="top-selling">
        <div class="container-fluid">


            @if (!string.IsNullOrEmpty(Model.Title))
            {
                <h1 class="collection-title">@Model.Title</h1>
            }



            @if (Model.GridItems != null)
            {
                <div class="collection-grid">

                    @foreach (var item in Model.GridItems)
                    {
                        @if (item.Id != "18" && item.Id != "19" && item.Id != "20")
                        {
                            <partial name="/Views/Partials/_GridCollectionItem.cshtml" model="item" />
                        }
                    }

                </div>
            }




            @if (Model.LoadMore)
            {
                <div class="load-more">
                    <button class="btn-outline-white me-1"><i class="fa-solid fa-chevron-left"></i></button>
                    <button class="btn-outline-white ms-1"><i class="fa-solid fa-chevron-right"></i></button>
                </div>
            }


            @if (Model.GridItems != null)
            {
                <div class="collection-grid more-item">

                    @foreach (var item in Model.GridItems.Take(3))
                    {

                        <partial name="/Views/Partials/_GridCollectionItem.cshtml" model="item" />

                    }

                </div>
            }

        </div>
    </section>
}